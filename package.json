{
  "name": "openwispr",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source desktop app to transform voice into text (local).",
  "license": "MIT",
  "scripts": {
    "dev": "npm run tauri:dev",
    "build": "npm run tauri:build",
    "tauri:dev": "tauri dev | cat",
    "tauri:build": "tauri build | cat"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
